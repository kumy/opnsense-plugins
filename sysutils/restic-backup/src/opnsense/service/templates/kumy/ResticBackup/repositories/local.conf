#
# Automatically generated configuration.
# Do not edit this file manually.
#

# Local
{%- from 'kumy/ResticBackup/macros.jinja' import add_overrides %}

REPOSITORY_ENABLED={% if not helpers.empty('system.backup.restic.repositories.local.enabled') and system.backup.restic.repositories.local.enabled == "1" %}true{% else %}false{% endif %}

{% if not helpers.empty('system.backup.restic.repositories.local.enabled') %}
export RESTIC_REPOSITORY="{{ system.backup.restic.repositories.local.path }}"
{% endif %}

{{- add_overrides('system.backup.restic.repositories.local') }}
